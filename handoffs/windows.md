# Windows 交接记录

## 当前任务

- 日期：2026-08-29
- 功能：DS-009 / Windows W-009 显示器身份与原生 DDC 稳定性
- 分支：`codex/windows-ds-009-native-display-control`
- PR base：`codex/coord-ds-009-native-display-control`
- 协调基线：`53c2397011323cd941afe315e3a6881fe772299e`
- 实现提交：`fc95ea36e6653a91fa19e1ebe831601048914538`
- PR #47 枚举安全评审修复：`626e81a830f5c9e7e41443613e4865e91f2023db`
- PR：[#47](https://github.com/maizihk/DisplaySwitch/pull/47)，目标为协调分支，保持开放且禁止自动合并

## 根因与修复

- 原生枚举原先把同一系统物理接口返回的每个 `PHYSICAL_MONITOR` 句柄分别追加为配置项，并使用接口加下标作为 ID；设置页又只追加不删除，因此重复句柄、断开项和通用“显示器 1/2/3”名称会持续累积。
- 现在按稳定物理接口合并候选句柄；只有完整、可信且非空的枚举快照才协调实时集合并清理断开显示器及孤立映射。部分失败、空集合、短暂断开或休眠会保留全部显示器配置、USB 映射和协同映射。
- 仍连接或休眠后恢复的显示器保持逻辑 ID、用户名称、DDC 功能开关、托盘开关、输入源和缓存。旧的带下标原生 ID 在本机规范化匹配，不改变 schemaVersion 5。
- 设置页、USB 映射和托盘使用用户名称或 Windows 友好名称；同型号按稳定本机 ID 排序生成序号。原始硬件标识只留在本机配置，不写日志或交接。
- DDC 偶发失效路径原先存在多线程同时枚举/操作 Dxva2 句柄、枚举失败退化为空集合、固定 150 ms 外层重试，以及 `auto` 控制通道可能选择 CMM 的混合语义。现在 Dxva2 获取、VCP 调用和释放串行化；同一接口的候选句柄有界尝试并统一释放；枚举/读取/写入失败明确返回。只有明确失败才重新枚举一次，不在成功后重试，也没有固定 sleep 或无限重试。
- W-009 正式运行时只选择原生 Dxva2。ControlMyMonitor 源码和兼容配置字段没有删除，但原生不可用时绝不调用它。
- USB 学习原先永久排除学习开始时已存在的引用。现在跟踪“曾离开”的基线设备，使其重新接入时成为候选；候选确认前仍保持零网络和硬件副作用。

## 自动验证

- `Windows/build-windows.ps1` x64 Release 完整通过，并真实运行 `DisplaySwitcher.Tests.exe`。
- W-009 模拟测试通过：两台物理显示器不会因重复句柄变成四项；枚举重排、可信断开清理、部分失败保留、空集合保留、休眠后恢复、孤立映射清理、不同/相同型号名称、用户设置保留、句柄唯一释放、原生失败零 CMM、部分显示器失败隔离，以及 USB 初始存在、离开、重新接入、多候选、取消和 30 秒超时。
- 既有 DS-004 C-001 至 C-024、DS-005 检测、DS-007 Windows 回归继续通过。
- v2 公共回归通过 1 条 NFC、4 条认证、20 条消息和 6 条状态机向量；USB-001 至 USB-016 全部通过。
- `Windows/dist/` 含绿色版入口和完整 `runtime/`，总计 1.61 MiB，低于 20 MiB。
- 构建期间仅出现 NuGet 漏洞索引网络不可达警告；现有依赖恢复、编译、链接和测试均成功。

## 尚需实机验证

- 实际两台显示器是否只枚举两项，以及 Windows 友好名称和同型号本机序号是否符合预期。
- 原生 Dxva2 亮度、对比度、音量的连续读取/提交，单台失败隔离和句柄重建。
- 输入源切换是否稳定响应；失败提示是否准确，且不会调用 ControlMyMonitor。
- USB 学习开始前已连接设备的离开、重新接入、多候选选择、取消和超时交互。
- 设置页/托盘名称、实时增删、高 DPI 和窄窗口交互。
- 本任务未启动正式程序，未执行真实 UDP、USB、DDC、显示器唤醒、输入源切换、防火墙或系统设置操作。

## 范围

- 仅修改 `Windows/` 和本文件。
- 未修改 `macOS/`、`handoffs/macos.md`、`PROTOCOL.md`、`AGENTS.md`、根 README、`coordination/`、`specs/`、`contracts/`、`.github/`、版本号、tag 或 Release。
- 不自动合并 PR，不单独触发云端 CI。
