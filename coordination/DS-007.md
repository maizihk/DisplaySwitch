# DS-007：设置界面与 DDC 控制统一

- 功能编号和名称：DS-007 / 设置界面与 DDC 控制统一
- 任务类型：cross-platform
- 当前状态：READY_TO_MERGE
- 协调起始基线：`842fea2e2867500c40fe95eb6b7924afab9f122e`
- 公共行为：双端全部设置页面统一、即时持久化、简化显示器页、双端托盘 DDC 控制、macOS 视觉修正、v2 状态修正和连续 DDC 写入恢复
- 协议 version：`version = 2`；用户已批准先将 `PROTOCOL.md` 更新为 v2-only
- 数据 schemaVersion：已批准升级为 `schemaVersion = 4`，不迁移旧配置
- proposal：`specs/proposals/DS-007-settings-and-ddc-ui-alignment.md`
- contract：只保留 `contracts/protocol-v2/`；用户已批准移除 `contracts/protocol-v1/`
- 集成分支：`codex/coord-ds-007-v2-only`
- 集成策略：两端平台分支从集成分支创建，并将 PR 目标设为该集成分支；两端通过后再以一个完整 PR 合入 `main`

## Windows

- 任务范围：按公共规范统一全部设置页、独立关于页、即时持久化、显示器页简化、每项托盘 DDC 开关/调节、schema v4 和 v1 删除。
- 分支：`codex/windows-ds-007-settings-ui`；最终提交：`d5d5f06b2e22d85ae0e1197a7cd0661b95536b1f`；PR：`#37`，已合入集成分支，合并提交 `980fb3f084e7f199975ba54d18563f73778cd78a`。
- 构建、测试和 CI：原生自动测试、x64 Release 构建、绿色版结构/体积检查通过；最终 HEAD 的 Windows CI run `33189219334` 通过。

## macOS

- 任务范围：按公共规范统一全部设置页、显示器页 DDC 控件补齐、联动开关迁移、即时持久化、标题居中、动态背景、按配置的 v2 状态、连续 DDC 写入合并/恢复、schema v4 和 v1 删除。
- 分支：`codex/macos-ds-007-settings-ui`；最终提交：`11e0cc1e7118fcf6da8cb7b6633834d52d5bc96e`；PR：`#36`，已合入集成分支，合并提交 `46b96541636e0482f6700ccaf9aab13f6e2957ac`。
- 构建、测试和 CI：47 项 XCTest、Debug/Release、打包和严格签名验证通过；最终 HEAD 的 macOS CI run `33188255346` 通过。

## 公共测试向量

- 提案定义 U-001 至 U-032；两端对应自动测试均通过，真实 GUI、网络和硬件场景另列为实机验证。
- `contracts/protocol-v2/` 的 HMAC 和消息向量保持不变。
- DS-007 v2-only 审计发现并移除 P-014/P-015 两条历史 v1 状态向量；当前公共状态机向量为 18 条，版本拒绝由消息向量和平台入口测试覆盖。

## 已发现差异

1. Windows 有关于弹窗代码但没有独立关于标签。
2. macOS 设置页没有完整的三项 DDC 滑杆和每项托盘显示开关。
3. macOS 联动开关位于常规页，Windows 位于显示器页。
4. 两端仍使用全局取消/保存，且协同检测确认文案依赖再次保存。
5. macOS 标题和浅色内容背景不符合用户要求。
6. Windows 托盘没有 DDC 调节入口及对应显示开关。
7. 当前 `PROTOCOL.md`、两端运行时和公共回归仍要求 v1 兼容，与用户的新决定冲突。
8. macOS 初始状态仍由 v1 选择逻辑生成，可能长期显示旧配置名、“等待心跳”或不准确的“无响应”。
9. macOS DDC 滑杆没有按显示器/VCP 合并连续写入，原生传输句柄失败后也没有明确的失效重建压力验收。
10. Windows 当前 DDC 开关重复显示标签、开关状态和 VCP 编号，并让简单开关占用过多垂直空间；两端需要统一紧凑的一项一行布局。
11. 两端五个设置页的信息层级、用词、字段顺序、密度和状态反馈尚未形成同一套产品规范。

上述实现差异已由 PR #36 和 #37 收敛。公共 v2 合同原有 P-014/P-015 两条 v1 兼容向量也已删除；两端直接执行当前 18 条 v2-only 状态机向量，不再保留过滤或特判。

## 用户决定

- 用户于 2026-08-28 批准提案末尾五项决定及双端全部设置页面统一范围。
- 用户于 2026-08-28 进一步批准现在先更新并合并 `PROTOCOL.md` 为 v2-only，同时删除 `contracts/protocol-v1/`。

## 集成约束

- 当前两端测试仍直接读取 v1 contracts；仅把共享删除提交合入 `main` 会造成已知构建失败。
- 为保持每个 `main` 提交可构建，共享规范和删除先提交到 `codex/coord-ds-007-v2-only`，Windows 与 macOS 从该分支并行实施。
- 两个平台 PR 均以集成分支为 base，且分别只修改自身平台目录和 handoff；不得修改共享文件。
- 两端 CI 通过并完成协调验收后，集成分支整体合入 `main`，期间不发布不完整中间版本。

## 仍需实机验证

- 双端设置页面真实交互、即时保存和重启恢复。
- macOS 浅色/深色背景及标题居中。
- 两端托盘滑杆、DDC 回读准确性、单项与联动写入；macOS 连续快速调节及失败恢复。
- 真实 v2 双端协同以及 v1 报文拒绝。

## 回滚方式

- 协调提案可独立回退，不改变当前协议和平台代码。
- 后续两端 PR 分开回退；旧配置备份必须保留。

## 合并建议

`READY_TO_MERGE`：两个平台 PR 已在相同 v2-only 基线上通过最终 CI 并合入集成分支；可创建集成 PR，在最终 macOS 与 Windows CI 均通过后合入 `main`。实机验证仍是正式 Release 的前置条件。
