# DS-009 显示器身份与原生 DDC 稳定性

- 功能编号和名称：DS-009 显示器身份与原生 DDC 稳定性
- 任务类型：cross-platform
- 当前状态：MACOS_IN_PROGRESS
- 当前基线：`main@0bbfa9e0fad8350462b3b68083aace4ca9063dce`
- 公共行为：显示器必须使用可辨识名称；DDC 原生接口失败必须明确报告，不得静默成功或切换到第三方后端
- 协议 version：保持 `version = 2`，无网络协议变更
- 数据 schemaVersion：保持 `schemaVersion = 5`，无公共数据变更
- proposal：无；现有规范已足够
- contracts：现有 `contracts/protocol-v2/` 与 `contracts/usb-switch-v1/` 不变

## 公共行为定义

1. 设置页和托盘不得只显示“显示器 1/2/3”；优先显示系统枚举的产品名称或用户保存的名称。
2. 同型号显示器必须提供稳定、仅本机可见的区分信息，不向网络、公共样例或日志暴露原始硬件标识。
3. 当前验证阶段只使用平台原生 DDC：Windows 不回退 ControlMyMonitor，macOS 不回退 m1ddc。
4. 原生读取、写入或枚举失败必须明确返回失败；不得用缓存值或第三方后端伪装为本次原生调用成功。
5. 自动测试只使用模拟后端，不访问真实 DDC、USB、网络、唤醒或输入源切换。

## Windows

- 任务范围：实时显示器集合去重与失效项清理、友好名称、原生 Dxva2 稳定性、USB 学习的离开后重新接入识别。
- 分支：待创建 `codex/windows-ds-009-native-display-control`
- 提交和 PR：待平台 Agent 提交。
- 构建、测试和 CI：待平台 Agent 本地验证；云端 CI 只在最终 main PR 运行。

## macOS

- 任务范围：设置页与菜单使用真实/自定义显示器名称；同型号可区分；禁用 m1ddc 自动回退；定位并修复原生 DDC 连续调节后失效的根因。
- 分支：待创建 `codex/macos-ds-009-native-display-control`
- 提交和 PR：待平台 Agent 提交。
- 构建、测试和 CI：待平台 Agent 本地验证；云端 CI 只在最终 main PR 运行。

## 未解决差异与阻塞

- Windows 实机枚举到 4 台而实际为 2 台，尚待平台实现修复并由用户复测。
- Windows 原生 DDC 切换多数无响应，尚待平台实现定位；本轮不允许用 ControlMyMonitor 掩盖失败。
- Windows USB 学习无法识别学习开始前已经存在、随后离开并重新接入的设备。
- macOS 连续多次 DDC 调节后原生接口失效，尚待定位资源生命周期、并发或写入节流根因。

## 仍需实机验证

- 两端显示器名称和同型号区分。
- Windows 实际显示器数量、Dxva2 输入源切换和 USB 学习。
- macOS 原生 DDC 亮度、对比度、音量读取，以及连续调节稳定性。

平台开发任务不得自行执行上述真实硬件动作；由用户安装最终测试包后验证。

## 回滚

分别回退 DS-009 平台提交；协议、schemaVersion 和现有本机配置不需要迁移或回滚。

## 合并建议

`NOT_READY`：已确认问题和原生接口验证边界，平台修复尚未完成。
