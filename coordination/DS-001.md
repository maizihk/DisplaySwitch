# DS-001 协议 v1 交接状态机一致性与公共测试向量

## 基本信息

- 功能编号：DS-001
- 名称：协议 v1 交接状态机一致性与公共测试向量
- 任务类型：cross-platform
- 当前状态：PROTOCOL_REVIEW
- 协调基线：`0fed95d4df54536889e1ec6b5a2c54db254c64c5`
- 协调分支：`codex/coord-ds-001`
- 公共协议：`PROTOCOL.md`，`version = 1`
- 公共数据：contracts 使用 `schemaVersion = 1`；不新增跨端配置同步
- 提案：`specs/proposals/DS-001-protocol-v1-state-machine-conformance.md`
- contract：`contracts/protocol-v1/` 下的消息 schema、向量 schema 和公共测试向量，`schemaVersion = 1`

## 公共行为定义

DS-001 不新增用户功能，也不改变现有协议 v1 的消息字段、端口、正常交接顺序或超时时序。目标是把现有协议中的共同要求变成两端都可用模拟时间和模拟硬件验证的状态机行为：

1. 非法、过期、未知版本、错误方向或认证失败的消息不得刷新对端在线状态，也不得触发唤醒、USB、DDC 或状态机迁移。
2. 合法消息可刷新对端在线状态；`status_probe` 必须以相同 `eventID` 回复 `status_response`，且状态探测不得产生硬件动作。
3. 在线源端按 150 ms 间隔最多发送 4 次 `handover_request`，并在 600 ms 后兜底；离线源端发送一次后立即切屏。
4. 重复请求不得重复唤醒或切屏，但目标端可以为当前同一事件重新发送确认，以容忍确认丢包。
5. USB 在等待期间回到源端时取消当前交接；旧事件的确认或提交不得影响新事件。
6. `usb_present` 表示物理 USB 已到达目标端，可以完成源端当前仍在等待的交接；其事件 ID 不要求等于源端尚未被目标端获知的事件 ID。
7. 迁移或验证失败时进入不执行硬件动作的安全状态。

详细规范、兼容策略和测试样例见已批准提案。提案批准授权协调 Agent 创建公共 contracts，但在协调 PR 合并前仍不授权平台修改行为。

## 协议与公共数据状态

- 网络消息版本继续使用 `version = 1`。
- contracts 使用 `schemaVersion = 1`；该字段不进入网络消息或平台本机配置，也不同步任何配置或设备信息。
- 不修改已发布字段含义。
- 未知 JSON 字段计划按向前兼容原则忽略；未知消息类型和未知协议版本计划安全拒绝。
- 原始显示器 UUID、USB 标识、IP、配对码、本机路径和个人硬件信息不得进入提案、contract、测试向量或日志。

## Windows 任务

### 范围

- 对应清单：W-003。
- 计划分支：`codex/windows-ds-001-state-machine`。
- 允许修改：`Windows/`、`handoffs/windows.md`。
- 从 `Controller` 分离纯交接状态机，注入时钟、UDP、USB、唤醒和 DDC 接口。
- 消费获批的公共测试向量，不修改共享提案、contract 或协议文件。
- 保持原生 C++/WinUI 3、现有 USB/UDP/DDC 和设置存储行为。

### 基线与交付状态

- 当前已核实基线：`main@0fed95d`，包含 W-001 提交 `980befd`。
- 分支：待创建。
- 提交：待提交。
- PR：待创建。
- 自动测试：当前没有 Windows 状态机自动测试工程。
- 构建：W-001 清单记录 x64 Release 本地构建通过，本轮未重新运行 Windows 构建。
- CI：当前仓库没有 Windows GitHub Actions 工作流。
- handoff：当前 `main` 中不存在。远端 `codex/windows-w002@fe7d7df` 已由 Windows Agent 新增该文件；须在 PR #2 合并后再按 `main` 事实核验，协调 Agent 不代写。
- 并行任务：Windows W-002 的 PR #2 已打开，head 为 `fe7d7df`；其变更限于 `Windows/` 和 `handoffs/windows.md`，未触碰 DS-001 共享文件。GitHub Actions run `33043830518` 已通过。

## macOS 任务

### 范围

- 对应清单：M-004。
- 计划分支：`codex/macos-ds-001-state-machine`。
- 允许修改：`macOS/`、`handoffs/macos.md`。
- 从 App/UI/硬件控制分离纯交接状态机，注入时钟、UDP、USB、唤醒和 DDC 接口。
- 消费获批的公共测试向量，不修改共享提案、contract 或协议文件。
- 保持 Swift/AppKit、现有 USB/UDP/DDC 和设置存储行为。

### 基线与交付状态

- 当前已核实基线：`main@0fed95d`，包含 M-003A 实现 `1a0faab` 和验证记录 `1405b1b`。
- 分支：待创建。
- 提交：待提交。
- PR：待创建。
- 自动测试：已有消息校验、重放保护和 JSON 往返测试；尚无完整的虚拟时间状态机测试。
- 构建与 CI：`main@0fed95d` 的 macOS Actions run `33042522510` 成功。
- handoff：`handoffs/macos.md` 存在，其 M-003A 分支、提交、PR 和 CI 记录已核实。

## 公共测试向量

协调 Agent 已在 `contracts/protocol-v1/` 创建公共 schema 和测试向量，当前包含 17 条消息验证向量与 15 条状态机向量，覆盖：

- 合法消息与六种消息类型。
- 缺失字段、错误字段类型、未知字段、未知消息类型和未知版本。
- 非 UUID `eventID`、错误 source/target、错误配对码、过期及未来超窗时间戳。
- 十秒有效期边界。
- 重复、乱序和跨事件的迟到确认。
- 150 ms 防抖、4 次请求重发、600 ms 兜底、6 秒在线窗口。
- USB 返回取消、USB 先到、请求先到、确认丢包和对端离线。
- `status_probe/status_response` 的相同 `eventID` 和零硬件副作用。

向量只使用固定测试 UUID、示例配对码、相对时间和逻辑端点，不包含真实设备或网络信息。`python3 contracts/protocol-v1/validate.py` 已通过结构、引用、时序和副作用不变量验证；两端原生测试尚未开始。

## 当前已发现差异

1. macOS 会验证 `eventID` 可解析为 UUID、时间戳有限且本地配对码至少 8 位；Windows 接收路径目前没有全部等价校验。
2. macOS 使用 `type + eventID` 记录重复消息，并对旧 `handover_request` 做乱序判断；Windows 目前只比较最新请求时间戳，相同事件的重发可能再次执行唤醒。
3. macOS 的消息类型是封闭枚举，未知类型在解码时失败；Windows 当前会在分派类型前刷新对端在线状态。
4. macOS 已有部分协议单元测试；Windows 尚无对应自动测试工程。
5. 两端均实现主要时序，但没有由同一组公共向量证明行为一致。

## 已批准决定与剩余评审

用户已于 2026-08-27 批准提案列出的五项决定：严格拒绝非法消息、重复请求幂等、`usb_present` 事件关联、合法消息刷新在线窗口，以及创建脱敏公共 contracts。

Windows W-002 可按纯平台任务继续在独立 PR #2 推进，因为当前差异未修改公共协议、公共协调文件或 macOS 源码。剩余评审是确认本轮生成的 contracts 准确表达已批准决定；在协调 PR 合并前不得下发 W-003/M-004 实施。

## 仍需实机验证

DS-001 的协议一致性验收不依赖真实硬件。平台实现完成后可另行安排一次双机冒烟测试，但未经用户在对应任务中明确确认，不执行真实 DDC、USB 交接、显示器睡眠/唤醒或可能黑屏的命令。

## 回滚方式

- 提案阶段：撤销协调分支中的 DS-001 文档，不影响运行时代码。
- 平台实施阶段：分别回退对应平台 PR；两端继续使用当前 `PROTOCOL.md` v1。
- 任一端新状态机验证失败时，不启用新路径，不执行硬件动作，并保留旧配置。

## 合并建议

当前结论：NOT_READY。

原因：提案已经批准，contracts 已创建并通过协调侧校验，但尚未完成用户复核和合并，两端平台任务也尚未开始。协调 PR 可以在 contracts 获确认后建议合并；当前不得修改 `PROTOCOL.md` 或平台实现。
