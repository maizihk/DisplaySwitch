# DS-004 跨平台功能对齐

- 功能编号和名称：DS-004 跨平台功能对齐
- 任务类型：cross-platform
- 当前状态：READY_FOR_PLATFORM（本机模型/UI 与双端 DDC 阶段已合并；Windows USB 学习/关于页面及 macOS 最终入口文案/C-023 自动验收待实施）
- 当前基线：`30b6d6966c38c155d30db6435c6b4b29e55f1c31`

## 目标和边界

核对 macOS 与 Windows 当前真实实现，区分必须完全一致的公共行为、语义应一致但实现可不同的能力、合理的平台差异以及需要用户决定的产品范围。

本审计不要求两端复制相同 UI，不修改平台代码、handoff、开发清单、`PROTOCOL.md`、contracts、GitHub Actions、Release、tag 或版本号，也不执行真实 USB、DDC、显示器唤醒或防火墙操作。

## 协议和公共数据状态

- 网络协议：`version = 1`，不变。
- 公共 contracts：`schemaVersion = 1`，不变。
- 已批准 proposal：`specs/proposals/DS-001-protocol-v1-state-machine-conformance.md`。
- 已批准 proposal：`specs/proposals/DS-004-display-controls-and-manual-switch-alignment.md`。
- 公共向量：17 条消息验证向量、16 条状态机向量，两端均已接入。
- DS-004 当前不提出网络字段或已发布数据语义变化。
- 用户已批准把多目标网络协同拆为 DS-005；DS-005 proposal 和 `contracts/protocol-v2/` 已通过 PR #18 合并为 `af8c707`。DS-004 不改变或复用 v1 消息语义。

## 当前协调分支和 PR

- 已批准提案分支：`codex/coord-ds-004-controls`
- 已批准提案 PR：[#17](https://github.com/maizihk/DisplaySwitch/pull/17)，合并提交 `8466c120c15607e7f39645c494b2786eac1f12ac`。
- 当前记录分支：`codex/coord-ds-004-phase2`。
- macOS 第一阶段：PR [#20](https://github.com/maizihk/DisplaySwitch/pull/20)，合并提交 `90e2d516c1cfac25b9efb45a18329f00c10a50ad`。
- Windows 第一阶段：PR [#21](https://github.com/maizihk/DisplaySwitch/pull/21)，合并提交及当前 `main` 为 `016ae59f502af6c6f3fe1f312888dbba41b0fd45`。
- 合并后主线 CI：macOS run `33089992256` 成功；Windows run `33090030891` 成功。
- macOS DDC 后端阶段：PR [#23](https://github.com/maizihk/DisplaySwitch/pull/23)，合并提交 `00bceff9e49f27f23c654d04a067b3168af6bbee`；合并后 macOS run `33094077255` 成功。
- Windows DDC 控制阶段：PR [#24](https://github.com/maizihk/DisplaySwitch/pull/24)，合并提交 `53c2197a75ee46a50ee1bdf31388345325ead0f5`；合并后 Windows run `33095681783` 成功。
- DS-006 平台公开文档：macOS PR #26 与 Windows PR #27 已合并；不改变 DS-004 运行时行为。

## 功能对齐矩阵

| 能力 | macOS 当前事实 | Windows 当前事实 | 判断 | 后续动作 |
| --- | --- | --- | --- | --- |
| UDP v1 消息与状态机 | 已接入 17+16 公共向量 | 已接入 17+16 公共向量 | 已对齐 | 保持公共向量为合并门槛 |
| 心跳角色 | 按规范回复 `status_probe` | 按规范每 2 秒发起 probe | 合理非对称 | 不改协议 |
| USB 防抖、重试、600 ms 兜底、离线降级 | 纯状态机实现 | 纯状态机实现 | 已对齐 | 后续改动必须双端回归 |
| 新安装安全默认值 | 不预设新显示器输入源，不自动启用硬件动作 | 不预设 USB、显示器、路径或输入源，不自动启用硬件动作 | 已对齐 | 保持平台测试 |
| 动态多显示器与稳定匹配 | UUID 匹配、任意数量、旧双显示器迁移 | 稳定 ID、任意数量、旧双显示器迁移 | 用户语义已对齐 | Windows 高 DPI/交互仍需实机确认 |
| 本机配置版本与迁移失败安全 | 本机 `schemaVersion = 3`；兼容 v2、v1 数组和旧键，未知版本、损坏、编码/写入失败持久进入安全状态 | 本机 `schemaVersion = 3`；兼容 v2，解析、编码、临时写入、完整回读或原子替换失败均保留旧文件并持久进入安全状态 | 已对齐并合并 | 后续改动继续覆盖当前实例和重启后的四类零副作用 |
| DDC 输入源切换 | Apple Silicon 原生后端，`m1ddc` 自动回退 | Dxva2 原生后端，可选 ControlMyMonitor | 合理平台差异 | 对齐能力/错误/取消接口语义，不强制相同后端 |
| DDC 后端接口化与模拟测试 | 原生后端与 `m1ddc` 回退已接口化 | Dxva2 与 ControlMyMonitor 已接口化 | 已对齐并合并 | 保持 C-016 至 C-020、C-024 为回归门槛 |
| 亮度、对比度、音量和 DDC 回读 | 已完成菜单控制、缓存和全零异常保护；真实原生读数未实机验证 | 已完成三项控制、回读、缓存和失败隔离 | 自动行为已对齐 | 两端真实读数、写入、失败隔离和高 DPI/交互仍待实机验证 |
| 手动切换入口 | 已移除切换到本机，按完整且已开启的配置名称生成入口 | 不提供切换到本机，按完整且已开启的配置名称生成入口 | 已对齐并合并 | DS-005 实施前只执行所选配置的本机 DDC 映射，不伪装 v2 手动协同 |
| 多协同配置 | schema v3 支持添加、命名、排序、删除和多个同时开启 | schema v3 支持添加、命名、排序、删除和多个同时开启 | 本机模型已对齐并合并 | 多目标网络选择仍等待 DS-005 运行时 |
| 显示器与输入映射 | 全局显示器目录；每个配置按稳定显示器 ID 保存对端输入 | 全局显示器目录；每个配置按稳定显示器 ID 保存对端输入 | 已对齐并合并 | 孤立映射保留并提示，不自动绑定新显示器 |
| USB 设备学习 | 可记录基线并根据一次 USB 变化列出候选 | 只能刷新并选择当前设备 | 功能未对齐 | Windows 增加安全学习流程；学习期间不得触发自动交接或硬件动作 |
| 关于页面 | 设置中已有关于页面 | 设置中没有关于页面 | 功能未对齐 | Windows 增加关于页；macOS 核对公共信息与隐私边界 |
| 登录启动 | `SMAppService` / 系统方式 | Windows 登录启动 | 用户语义已对齐 | 保留平台原生实现 |
| 对端连接状态 | 设置窗口显示连接状态 | 设置窗口显示连接状态 | 已对齐 | 未来诊断页统一字段语义 |
| 诊断与脱敏 | 主要使用状态机日志和 `NSLog`，没有诊断页面或导出预览 | 有不含配置值的基础 `diagnostic.log`，没有诊断页面或导出预览 | 双端共同欠缺，成熟度不同 | 先提案定义脱敏诊断 schema，再实施 M-006/W-203 |
| 首次运行向导 | M-201 未完成 | W-202 未完成 | 双端共同欠缺 | 在 DDC 接口和诊断之后设计公共用户流程 |
| 平台限制文档 | 根 README 已概述；M-007 未完成 | Windows README 已有基础说明；W-005 未完成 | 部分对齐 | 两端分别补齐硬件限制和诊断安全文档 |
| CI 与测试 artifact | Debug、测试、Release、临时签名验证 | x64 Release、测试、dist 检查 | 已对齐到平台合理范围 | artifact 均不得描述为正式 Release |
| 正式签名、安装、发布 | M-101/M-102 未完成 | W-101/W-102 未完成 | 双端共同欠缺 | 各平台独立推进，不互相阻塞 |
| 多目标协议 v2 | v1 固定 mac/windows 角色和 USB 语义 | v1 固定 mac/windows 角色和 USB 语义 | 无法表达手动唤醒、多目标发现和蓝牙到达 | 已拆分为 DS-005 提案；未经批准不得修改 v1 语义 |

## 审计发现的记录差异

1. macOS 记录漂移已由 PR #15 修复：M-004 清单与已合并实现、公共向量和 CI 结果一致。
2. Windows handoff 记录漂移已由 PR #14 修复：PR #9 合并提交和主线 Windows CI 已准确记录。
3. macOS 配置失败安全差距已由 PR #15 修复并合并；协调评审发现的“无配置文档时忽略持久安全标记”也已在合并前修复并增加回归测试。
4. macOS PR #20 在合并前修复 USB 学习目标串绑、15/30 秒规范偏差、学习期副作用闸门和个人路径/测试地址脱敏问题。
5. Windows PR #21 在合并前补齐正常 v3 设置保存各阶段失败的持久安全标记、当前进程立即停用和重启后安全状态测试。

平台 Agent 必须自行更新本平台清单和 handoff；协调 Agent 不代填或修改。

## 推荐实施顺序

### 第一阶段：恢复事实一致性和配置安全

已完成：

1. macOS Agent 通过 PR #15 更正 M-004 清单状态和 handoff。
2. Windows Agent 通过 PR #14 更正 W-004 handoff 的合并状态和最终 main CI。
3. macOS Agent 通过 PR #15 增加本机 `schemaVersion = 2`、v1/旧键兼容迁移、原数据保留、持久失败安全门和无硬件回归测试。

第三项不改变网络协议或公共数据语义，Windows 已有等价安全行为，因此 Windows 不应被阻塞。

### 第二阶段：共同能力语义

1. 已完成并合并：全局显示器目录、多协同配置、输入映射、检测按钮、角色中性菜单、本机 schema v3 迁移、安全失败以及 C-001 至 C-015。
2. 已完成：Windows W-201 与 macOS M-005 的 DDC 后端接口化、三项控制共同语义、失败隔离、取消、缓存和 C-016 至 C-020、C-024 模拟测试已由 PR #23/#24 合并。
3. 下一阶段：实施 Windows USB 学习和关于页面；macOS 清理残留固定平台入口/提示并增加关于页面 C-023 自动验收；覆盖 C-021 至 C-023 的适用路径。
4. 多配置手动/自动网络协同、目标发现和 USB/蓝牙到达消息已拆为 DS-005；proposal 和公共 contracts 已合并。v2 网络运行时仍等待 `PROTOCOL.md` 获明确授权更新。
5. DS-004 显示控制和学习/关于页面对齐完成后，再单独起草跨平台诊断提案。

### 第三阶段：产品范围和首次引导

1. 用户已决定 Windows 补齐亮度、对比度、音量和回读控制；显示器目录与多个协同配置分离；每端不提供切换到本机；Windows 补齐 USB 学习和关于页面。
2. 基于已稳定的配置、DDC 能力和诊断字段设计两端首次运行安全引导。
3. 正式签名/安装和协议 v2 分别另立任务，不混入功能对齐实现。

## Windows 任务范围

- 已完成范围：PR #21 已实现并合并本机 schema v3、全局显示器目录、多协同配置、输入映射、检测、配置名称菜单、迁移与保存失败安全测试。
- 已完成范围：W-201 DDC 后端接口化、亮度/对比度/音量与回读、缓存、取消、失败隔离及模拟测试已由 PR #24 合并。
- 后续实施：Windows USB 学习和关于页面及 C-021 至 C-023；v2 网络运行时等待现行协议更新后另行实施。
- 后续候选：W-005 文档与诊断安全、W-203 诊断页面。
- Windows 第一阶段分支、提交和 PR：`codex/windows-ds-004-local-model`，最终 head `a58ab54bd77c80b102275c88dd2b077ddc24809c`，PR #21，合并提交 `016ae59f502af6c6f3fe1f312888dbba41b0fd45`。
- Windows 构建、测试和 CI：PR head run `33087718824` 与合并后 main run `33090030891` 均成功；C-001 至 C-015、v1 17+16 向量、x64 Release、dist 和 artifact 验证通过。

Windows Agent 下一步提示词：

```text
功能编号：DS-004 / Windows USB 学习与关于页面收尾
当前基线：以合并 DS-004～DS-006 状态修正协调 PR 后的 main 为准
分支：codex/windows-ds-004-usb-about
允许修改：Windows/、handoffs/windows.md
禁止修改：macOS/、handoffs/macos.md、PROTOCOL.md、AGENTS.md、根 README、coordination/、specs/、contracts/、GitHub Actions、Release、tag、版本号

完整阅读 AGENTS.md、PROTOCOL.md、coordination/DS-004.md、coordination/DS-005.md、specs/proposals/DS-004-display-controls-and-manual-switch-alignment.md、contracts/protocol-v1/、Windows/DEVELOPMENT_CHECKLIST.md、Windows/README.md 和 handoffs/windows.md。

完成 DS-004 Windows 收尾：把现有 USB 枚举/选择扩展为绑定到稳定协同配置 ID 的真正 USB 学习流程。学习期以开始时设备集合为基线，30 秒内检测新增候选；多个候选必须让用户选择，不得猜测；取消、超时、目标配置删除或迟到结果必须保留原绑定。学习期间以及候选确认前，UDP、自动 USB 交接、DDC 和唤醒副作用必须为零。覆盖 C-021、C-022。

增加关于页面并覆盖 C-023：仅显示图标、应用名称、公开版本/架构/协议信息和项目链接，不读取或显示 IP、配对码、USB/显示器标识、路径或其他本机敏感数据；打开和交互不得触发网络或硬件动作。用户可见的手动入口和协同提示使用配置自定义名称或“对端”，不得硬编码目标为 Mac；不得改变 v1 source/target 字段、状态机时序、兼容迁移或自动返回本机语义。

自动测试使用模拟设备、模拟时间和临时配置，覆盖多候选、取消、30 秒超时、配置删除、迟到回调、关于页面数据源和全链路零副作用；继续通过 C-001 至 C-020/C-024 以及 v1 17+16 公共向量。运行 Windows/build-windows.ps1 x64 Release，检查 dist、敏感信息和构建产物。更新 handoffs/windows.md，区分自动验证与未执行实机验证，正常 commit、fetch、push 并创建 PR；不得 force push、合并、创建 tag/Release 或修改版本号。
```

## macOS 任务范围

- 已完成范围：PR #20 已实现并合并本机 schema v3、全局显示器目录、多协同配置、输入映射、检测、角色中性菜单、30 秒 USB 学习安全以及迁移失败安全测试。
- 已完成范围：M-005 DDC 后端接口化、能力/读写/错误/取消/失败隔离和三项全零模拟测试已由 PR #23 合并。
- 后续实施：清理固定平台手动入口及用户提示，增加关于页面 C-023 自动验收；v2 网络运行时等待现行协议更新后另行实施。
- 后续候选：M-006 诊断与脱敏、M-007 限制文档。
- macOS 第一阶段分支、提交和 PR：`codex/macos-ds-004-local-model`，最终 head `7f7cafe40ba2455ad0b39a2b25bab89c2de4c337`，PR #20，合并提交 `90e2d516c1cfac25b9efb45a18329f00c10a50ad`。
- macOS 构建、测试和 CI：PR head run `33089474995` 与合并后 main run `33089992256` 均成功；27 项配置测试、40 项 XCTest、v1 17+16 向量、Debug、Release、打包和严格验签通过。

macOS Agent 下一步提示词：

```text
功能编号：DS-004 / macOS 手动入口与关于页面收尾
当前基线：以合并 DS-004～DS-006 状态修正协调 PR 后的 main 为准
分支：codex/macos-ds-004-final-ui-about
允许修改：macOS/、handoffs/macos.md
禁止修改：Windows/、handoffs/windows.md、PROTOCOL.md、AGENTS.md、根 README、coordination/、specs/、contracts/、GitHub Actions、Release、tag、版本号

完整阅读 AGENTS.md、PROTOCOL.md、coordination/DS-004.md、coordination/DS-005.md、specs/proposals/DS-004-display-controls-and-manual-switch-alignment.md、contracts/protocol-v1/、macOS/DEVELOPMENT_CHECKLIST.md 和 handoffs/macos.md。

完成 DS-004 macOS 收尾：用户可见的手动切换入口只能按完整、已开启的协同配置自定义名称生成，不显示或保留可触发的“切换到 Mac/本机”入口。清理旧 switchToMacItem/switchItem 等仅服务旧手动入口的死代码；USB 自动返回本机、v1 交接、输入源映射、防抖、重试和超时语义必须保持。设置、状态和提示在可以使用配置名称时不得硬编码 Windows/Mac；协议 v1 的内部角色字段不得改变。

为现有关于页面增加 C-023 自动测试，证明只使用公开 Bundle/静态信息，不读取本机配置，不暴露 IP、配对码、USB/显示器标识或路径，且打开页面对 UDP、USB、DDC、唤醒和输入源切换均为零副作用。复核现有 USB 学习 C-021/C-022：多候选等待选择，取消/30 秒超时/配置删除/迟到结果保留原绑定且零自动副作用；缺口只在本任务范围内补齐。

运行相关 XCTest、全部 XCTest、Debug/Release、./macOS/scripts/build-app.sh 和 codesign --verify --deep --strict macOS/outputs/DisplaySwitcher.app。检查敏感信息和构建产物，更新 handoffs/macos.md，区分自动验证与未执行的 GUI/硬件验证，正常 commit、fetch、push 并创建 PR；不得执行真实 DDC/USB/唤醒，不得 force push、合并、创建 tag/Release 或修改版本号。
```

## 公共验收与实机验证

- 协调侧继续运行 `contracts/protocol-v1/validate.py`。
- 涉及交接行为时，两端必须继续通过全部 17+16 公共向量。
- 配置安全和诊断测试必须使用临时目录、模拟数据和模拟硬件接口。
- 未经用户当前任务明确确认，不执行真实 DDC、USB 交接、睡眠/唤醒或防火墙修改。
- 高 DPI、菜单/托盘交互、真实 DDC 兼容性、真实 USB 交接和睡眠唤醒仍须明确标记为实机验证。

## 兼容、迁移和降级策略

- 两端本机配置已经迁移至 `schemaVersion = 3`；该版本仅用于本机数据，不进入网络消息。
- macOS 读取现有 v2、`Displays.Configuration.v1` 数组和旧双显示器键；失败时保留原数据并持久停用自动硬件动作，只有用户成功保存才能解除。
- 未知配置版本必须安全拒绝，不能猜测字段语义或覆盖用户配置。
- 平台专有后端不可用时应报告可用性并安全降级，不得误操作其他显示器。
- Windows v3 已建立全局显示器目录和旧对端协同配置；v2 `MacInput` 原值迁入该配置的 `peerInput`，全局 `localInput = null`。
- macOS v3 已将 v2 `macInput` 迁为全局 `localInput`，将 `windowsInput` 迁入旧对端配置的 `peerInput`。
- 任一平台 v3 写入失败时必须保留原 v2 数据并进入持久安全状态；名称和角色字段只保存在本机。

## 需要用户决定的问题

已决定：

1. Windows 增加与 macOS 对齐的亮度、对比度、音量和 DDC 回读控制。
2. macOS 移除切换到本机；Windows 不新增切换到本机；菜单按协同配置名称生成对端入口。
3. 两端采用全局显示器目录和多个协同配置；配置可同时开启，网络目标选择由 DS-005 定义。
4. Windows 补齐 USB 学习和关于页面。
5. Dell 显示器切换到 macOS 原生 DDC 接口后的读数准确性尚未实机验证，不能提前标记解决。
6. 多目标网络协同拆分为 DS-005 协议 v2 提案。

2026-08-27 用户已批准 `specs/proposals/DS-004-display-controls-and-manual-switch-alignment.md` 中的详细行为、双端本机 schema v3 迁移和 C-001 至 C-024 验收样例。

## 回滚方式

协调阶段回退本文件和 DS-004 提案即可。后续平台任务必须在各自独立分支提供自己的回滚说明；Windows 平台回滚还需显式处理本机 schema v3 配置兼容。

## 合并建议

当前为 `READY_FOR_PLATFORM`：DS-004 本机模型/UI 与双端 DDC 阶段 PR #20/#21/#23/#24 均已合并，相关主线 CI 成功。剩余合并门槛是 Windows USB 学习/关于页面、macOS 固定平台入口与提示清理，以及 C-021 至 C-023 的适用自动验收。DS-005 v2 运行时不属于本阶段。
